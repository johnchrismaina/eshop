import React from 'react';

const CreateShop = ({
  sellerId,
  setActiveStep,
}: {
  sellerId: string;
  setActiveStep: (step: number) => void;
}) => {
  return <div>
    const {
        register,
        handleSubmit,
        formState: {errors},
    } = useForm()
  </div>;
};

export default CreateShop;
