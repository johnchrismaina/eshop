import React from 'react';

const CreateShop = ({
  sellerId,
  setActiveStep,
}: {
  sellerId: string;
  setActiveStep: (step: number) => void;
}) => {
  return <div>
    
  </div>;
};

export default CreateShop;
